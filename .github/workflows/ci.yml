name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'uat'
        type: choice
        options: [uat, production]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Install Node dependencies
        run: npm ci

      - name: Set NX_BASE and NX_HEAD
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "NX_BASE=origin/${{ github.base_ref }}" >> $GITHUB_ENV
            echo "NX_HEAD=${{ github.sha }}"             >> $GITHUB_ENV
          else
            echo "NX_BASE=${{ github.event.before }}"   >> $GITHUB_ENV
            echo "NX_HEAD=${{ github.sha }}"             >> $GITHUB_ENV
          fi

      - name: Sync affected Python envs
        run: npx nx affected -t sync --base=$NX_BASE --head=$NX_HEAD --parallel=4

      - name: Lint affected
        run: npx nx affected -t lint --base=$NX_BASE --head=$NX_HEAD --parallel=4

      - name: Verify wheel-bundled package versions are bumped on source changes
        run: python3 scripts/check_wheel_versions.py --base=$NX_BASE --head=$NX_HEAD

      # Repo-shape contract checks: target-shape, watch-mode test drift,
      # rendered app.yaml against deploy.toml. Documented in
      # docs/architecture/repo-consolidation.md as the source-of-truth
      # health command.
      - name: check:repo
        run: npm run check:repo

      # Frontend design-system audit: --strict fails the build on any
      # @carbon/* import (forbidden by frontend_rules.md). Tailwind usage
      # is reported as a warning until the SPC migration question is
      # answered (see TODO.md).
      - name: audit:frontend (strict — Carbon forbidden)
        run: npm run audit:frontend:strict

      # Dedicated architecture linters. Python import contracts live in
      # .importlinter; TypeScript module boundaries use @nx/enforce-module-
      # boundaries with project tags from project.json.
      - name: Architecture boundary lint
        run: |
          npm run lint:architecture:python
          npm run lint:architecture:ts

      # Semantic-model governance — every tbl()/silver_tbl() call must reference
      # an entity in ai-context/semantic-model/entities.yaml (or the
      # APPROVED_BUT_UNLISTED ratchet list). Also asserts no hard-coded
      # `connected_plant_uat` catalog references in Python or SQL view DDL.
      - name: Semantic-model governance
        run: uv run --with pytest --with pyyaml pytest scripts/tests/test_semantic_model_governance.py --tb=short

      # OpenAPI drift check — regenerate every backend's openapi.json in a
      # temp directory and diff against the committed copy. Fails the build
      # if a backend Pydantic schema changes without a regen. Frontend types
      # are generated locally via `npm run openapi:generate`; this gate is
      # spec-only because npx codegen needs npm install (already done above).
      - name: OpenAPI spec drift check
        run: uv run --no-sync python scripts/generate_openapi.py --check

      # Docstring presence gate. Each pyproject.toml has a [tool.interrogate]
      # block with fail-under set at the current floor (see TODO.md ratchet
      # entry). Regression below that floor fails CI; ratchet up sprint by
      # sprint as the team adds docstrings.
      - name: Docstring presence (interrogate)
        run: |
          uv tool install interrogate
          set -e
          for cfg in apps/connectedquality/backend apps/envmon/backend \
                     apps/processorderhistory/backend apps/spc/backend \
                     apps/trace2/backend apps/warehouse360/backend \
                     libs/shared-api libs/shared-auth libs/shared-db libs/shared-domain; do
            echo "::group::interrogate $cfg"
            uv tool run interrogate --config "$cfg/pyproject.toml" "$cfg"
            echo "::endgroup::"
          done

      # Databricks bundle validation — catches malformed databricks.yml,
      # missing resource references, and rendered app.yaml issues at PR time
      # rather than at deploy time. We run --target uat against every bundle;
      # cheap (no API calls beyond syntax/resource resolution) and gives a
      # fast signal that deploys will not fail closed at the wrong moment.
      - uses: databricks/setup-cli@v0.240.0
      - name: Databricks bundle validate (all apps)
        env:
          DATABRICKS_HOST: https://example.cloud.databricks.com
          DATABRICKS_TOKEN: dummy-token-for-syntax-validation
        run: |
          set -e
          for bundle in apps/*/databricks.yml; do
            app_dir=$(dirname "$bundle")
            echo "::group::bundle validate $app_dir"
            (cd "$app_dir" && databricks bundle validate --target uat) || exit 1
            echo "::endgroup::"
          done

      - name: Typecheck affected frontends
        run: npx nx affected -t typecheck --base=$NX_BASE --head=$NX_HEAD --parallel=4

      - name: Test affected
        run: npx nx affected -t test --base=$NX_BASE --head=$NX_HEAD --parallel=4

      - name: Upload coverage artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-reports
          path: |
            apps/*/backend/coverage.xml
            apps/*/frontend/coverage/
          retention-days: 14

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      # E2E smoke gate — runs @smoke-tagged tests only, with mocked API routes.
      # APP_ENV=test activates shared-auth dev-mode bypass (no JWKS required).
      # E2E_USE_FIXTURES=1 is consumed by individual tests that call mockAllApiRoutes.
      - name: E2E smoke tests (mocked — PR gate)
        env:
          APP_ENV: test
          E2E_USE_FIXTURES: '1'
        run: |
          npx nx affected -t e2e \
            --base=$NX_BASE --head=$NX_HEAD \
            --parallel=2 \
            -- --grep "@smoke"

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: |
            apps/*/e2e/playwright-report/
            apps/*/e2e/test-results/
          retention-days: 14

      - name: Build affected frontends
        run: npx nx affected -t build --base=$NX_BASE --head=$NX_HEAD --parallel=4

  security:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      # ----- Python: known-CVE scan via pip-audit ---------------------------
      # `uv sync` resolves and installs the workspace into a single venv that
      # covers every backend and shared lib. `uv run --with pip-audit` runs
      # pip-audit *inside* that env so we audit the actually-resolved
      # dependency tree (`uv tool run` would isolate pip-audit and miss the
      # project deps entirely).
      - name: pip-audit (Python dependencies)
        run: |
          uv sync --all-extras
          uv run --with pip-audit pip-audit --strict

      # ----- JavaScript: known-CVE scan via npm audit ----------------------
      # Workspace-aware; --workspaces flag would also work but the root audit
      # already walks the npm workspaces declared in package.json.
      - name: Install Node dependencies
        run: npm ci

      - name: npm audit (JavaScript dependencies)
        run: npm audit --audit-level=high

      # ----- Secret scanning via gitleaks ----------------------------------
      # Catches accidentally-committed credentials. Runs against the full
      # history on push to main (so a sneaky commit is found post-merge too)
      # and against the diff on PRs (faster, scoped to what changed).
      - name: gitleaks (secret scanning)
        uses: gitleaks/gitleaks-action@v2.3.9  # pinned from @v2 (floating major tag)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ----- SAST: GitHub CodeQL ------------------------------------------
      # Two languages — python for the FastAPI/uvicorn backends and the build
      # scripts; javascript covers all the frontend SPAs.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: python, javascript

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Run CodeQL analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:python,javascript"
          fail-on: high

  # Full E2E suite against live UAT Databricks workspace.
  # Runs in parallel per-app after ci + security pass on main.
  # Requires E2E_DATABRICKS_UAT_TOKEN secret in the "uat" GitHub environment.
  e2e-live:
    needs: [ci, security]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: uat
    strategy:
      matrix:
        app: [warehouse360, spc, trace2, platform, envmon, processorderhistory]
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
      - name: Install Node dependencies
        run: npm ci
      - name: Sync Python environments
        run: uv sync --all-packages

      # Gold-view contract test — every entity in entities.yaml must answer
      # SELECT * LIMIT 1 and return its declared important_dimensions columns.
      # Run once per matrix shard (cheap; the same warehouse for every app),
      # before Playwright so a schema drift fails fast.
      - name: Gold-view SQL contract smoke
        if: matrix.app == 'warehouse360'  # run once; not per-app
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.E2E_DATABRICKS_UAT_TOKEN }}
          DATABRICKS_WAREHOUSE_HTTP_PATH: ${{ secrets.DATABRICKS_WAREHOUSE_HTTP_PATH }}
          TRACE_CATALOG: ${{ secrets.UAT_TRACE_CATALOG }}
        run: uv run --with pytest --with pyyaml pytest scripts/tests/test_gold_view_contracts.py -v --tb=short

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
      - name: Run E2E — ${{ matrix.app }}
        env:
          APP_ENV: test
          E2E_DATABRICKS_TOKEN: ${{ secrets.E2E_DATABRICKS_UAT_TOKEN }}
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          PLAYWRIGHT_BLOB_OUTPUT_DIR: apps/${{ matrix.app }}/e2e/blob-report
        run: |
          npx playwright test \
            --config=apps/${{ matrix.app }}/e2e/playwright.config.ts \
            --reporter=blob \
            --workers=2
      - name: Upload blob report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: blob-report-${{ matrix.app }}
          path: apps/${{ matrix.app }}/e2e/blob-report/
          retention-days: 30

  # Merge all per-app blob reports into a single HTML report.
  e2e-merge-reports:
    needs: [e2e-live]
    if: always() && github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          pattern: blob-report-*
          merge-multiple: true
          path: all-blob-reports
      - run: npx playwright merge-reports --reporter html ./all-blob-reports
      - uses: actions/upload-artifact@v4
        with:
          name: playwright-merged-report
          path: playwright-report/
          retention-days: 30

  deploy:
    needs: [ci, security]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: production
    concurrency:
      group: deploy-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - uses: databricks/setup-cli@v0.240.0  # pinned from @main — verify SHA against https://github.com/databricks/setup-databricks-cli/releases

      - name: Install Node dependencies
        run: npm ci

      - name: Set NX_BASE
        run: |
          if [[ "${{ github.event.before }}" == "0000000000000000000000000000000000000000" ]]; then
            echo "NX_BASE=HEAD~1" >> $GITHUB_ENV
          else
            echo "NX_BASE=${{ github.event.before }}" >> $GITHUB_ENV
          fi

      - name: Deploy affected Databricks apps
        env:
          DATABRICKS_HOST:  ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          npx nx affected -t deploy \
            --base=$NX_BASE \
            --head=${{ github.sha }}
