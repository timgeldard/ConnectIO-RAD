/**
 * Shared platform query-catalog helpers.
 *
 * Slice 1 intentionally keeps this file minimal so the registry can move into
 * domain modules before the shared helper set is expanded in later slices.
 */
export {};
