{
  "name": "spc-backend",
  "projectType": "application",
  "tags": ["scope:spc", "type:backend"],
  "implicitDependencies": ["shared-db"],
  "targets": {
    "sync": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv sync --package spc-backend",
        "cwd": "{workspaceRoot}"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "PYTHONPATH=. uv run --package spc-backend uvicorn backend.main:app --reload --port 8000",
        "cwd": "apps/spc"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "PYTHONPATH=. uv run --package spc-backend pytest backend/tests",
        "cwd": "apps/spc"
      },
      "inputs": ["pyFiles", "sharedGlobals"],
      "cache": true
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "PYTHONPATH=. uv run ruff check backend && PYTHONPATH=. uv run mypy backend",
        "cwd": "apps/spc"
      },
      "inputs": ["pyFiles", "sharedGlobals"],
      "cache": true
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "databricks bundle deploy --target prod",
        "cwd": "apps/spc"
      }
    }
  }
}
