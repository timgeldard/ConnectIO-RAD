"""Domain layer for trace2 Genie Assist.

Empty at present — the wire protocol with Databricks Genie is fully
expressed in the application layer.  Reserved for future pure-domain
logic (e.g. lineage-specific prompt heuristics, attachment classifiers)
that would otherwise leak into the application layer.
"""
