{
  "name": "shared-app-context",
  "projectType": "library",
  "root": "libs/shared-app-context",
  "tags": ["scope:shared", "type:frontend"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "libs/shared-app-context"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "libs/shared-app-context"
      },
      "cache": true
    },
    "test": {
      "executor": "nx:run-commands"
    }
  }
}
