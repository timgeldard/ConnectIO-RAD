{
  "name": "shared-auth",
  "projectType": "library",
  "tags": ["scope:shared", "type:backend"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands"
    },
    "test": {
      "executor": "nx:run-commands"
    }
  }
}
