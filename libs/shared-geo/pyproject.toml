[project]
name = "shared-geo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pgeocode>=0.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared_geo"]
